{
  "$schema": "https://opencode.ai/config.json",
  "references": {
    "rules": {
      "path": ".opencode/rules",
      "description": "Persistent, reusable context at the prompt level"
    }
  },
  "plugin": ["@ramtinj95/opencode-tokenscope@latest"]
}