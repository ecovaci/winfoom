---
description: Implement a controller
agent: build
---

### /implement-endpoint
Use with the `@developer` agent for controller implementation.
Implement the REST endpoint for: $ARGUMENTS

Follow `@WebMvcTest` annotation conventions. Do not add business logic to the controller.

