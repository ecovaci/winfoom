---
description: Implement a service
agent: build
---

### /implement-service
Use with the `@developer` agent after the `@architect` has produced a spec.

**Implement the following service based on this spec:** $ARGUMENTS

Generate:
1. The service interface
2. The service implementation
3. Any new repository methods needed
4. Any new exception classes needed
